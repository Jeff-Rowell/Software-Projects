Tuesday Thursday Team 3GroupDocumentsViewModel 1. View Model Name : GroupDocumentsViewModel	a. Properties of the View Model 	    i. The simple pass-through properties.		private int groupSelectedLessonId		private int selectedLessonId		private int groupSelectedDocumentUseId		private int groupReferenceLessonId	    ii. The rest: the needed output properties for the View Object.	 	private string _heading; /// never used	    	private List<DocumentUseViewModel> _teachingDocumentUses;	    	private List<DocumentUseViewModel>_instructorOnlyDocumentUses;	    	private List<DocumentUseViewModel> _referenceDocumentUses;	    	private string _downloadPdfLabel;	    	private string _downloadDocumentLabel;	    	private string _downloadPdfToSaveLabel;	    	private bool _selectedDocUseIsPdf;	    	private bool _selectedDocUseHasPdfCopy;	    	private string _selectedDocumentUseDisplayName;	    	    						b. Note any logic in the View Model		No logic in view model2. Repos    a. List all repos being called by the factory		DocumentUseRepo    b. List the repo methods being called directly.		documentUseRepo.TeachingDocumentUsesForLesson		documentUseRepo.InstructorOnlyDocumentUsesForLesson		documentUseRepo.ReferenceDocumentUsesForLesson		documentUseRepo.GetById    c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this		They’re simple queries3. DbObjects   	a. List the DbObjects being used.		DocumentUse		Document	b. For each of these, list the properties being read.		DocumentUse.Document		Document.DisplayName		Document.HasPdfCopy		Document.IsPdf 		Document.Extension   	c. Identify any of these properties that are from custom code in the DbObject.		Document.DisplayName		Document.HasPdfCopy		Document.IsPdf		Document.Extension	d. For each these DbObjects, indicate whether your new ViewObject should contain a DomainObject for this or just list a couple of properties.		Keep it as a domain object, because it is used in a list and additional properties may be used later. 4. Other View Models	There are none-------------------------------------------------------------------------------GroupLessonDisplayViewModel1. View Model: GroupLessonDisplayViewModel	a. Properties of the View Model 	    private int _lessonId	    private string _lessonName	b. The simple pass-through properties.        	private int _lessonId		private string _lessonName	c.  The rest: the needed output properties for the View Object.    	d.  Note any logic in the View Model     		There is no logic in the View Model 2. Repos	a. List all repos being called by the factory 	       There are no repos being called	b. List the repo methods being called directly. 	       There are no methods being called directly	c. If any of these methods are not simple queries, but can be traced back to simple 	queries, briefly describe this. 	       There are none3. DbObjects	a. List the DbObjects being used. 	     Lesson	b. For each of these, list the properties being read.	     Lesson.Name	     Lesson.Id	c. Identify any of these properties that are from custom code in the DbObject. 	     None are custom code.	d. For each these DbObjects, indicate whether your new View Object should contain a DomainObject for this or just list a couple of properties.	     This view object can be eliminated. It will not be needed to populate the View Model.4. Other View Models          No other View Models are used in this Model-------------------------------------------------------------------------------GroupLessonPlanViewModel1. View Model Name: GroupLessonPlanViewModel   	 a. Properties of the View Model     		i. The simple pass-through properties.     			private int selectedLessonId		ii. The rest: the needed output properties for the View Object.			private string _heading;    			private string _lessonPlanText;    			private IEnumerable<SelectListItem> _groupLessonPlanSelectList;    	 b. Note any logic in the View Model		No logic in the viewmodel2. Repos    a. List all repos being called by the factory		LessonPlanRepo		LessonRepo    b. List the repo methods being called directly.		lessonRepo.getById		lessonPlanRepo.lessonPlansForLesson		lessonPlanRepo.lessonPlanForLesson    c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this.		LessonPlanRepo.GetLessonPlanForLesson calls LessonPlanRepo.GetPreferredLessonPlan to return a LessonPlan object which then calls LessonPlanLessonRepo.GetPreferredLPLForLesson which is a simple query.3. DbObjects    a. List the DbObjects being used.		Lesson		LessonPlan		Course		User    b. For each of these, list the properties being read.		Lesson.Course		Course.IsMaster		Course.User		User.DisplayName    c. Identify any of these properties that are from custom code in the DbObject.		Course.IsMaster is custom. It returns true if there is no Master for this course.    d. For each these DbObjects, indicate whether your new ViewObject should contain a DomainObject for this or just list a couple of properties.		It is difficult to make a determination on these objects at this point because the LessonPlan dbobject is not being used. The other objects are not being returned directly. -------------------------------------------------------------------------------GroupLessonsViewModel1.	View Model Name: GroupLessonsViewModel	a.	Properties of the View Model		i.  The simple pass-through properties			private int _selectedLessonId		ii. The rest: the needed output properties for the View Object			private List<GroupLessonDisplayViewModel>			_groupLessonDisplayVMList		        private string _heading	b. Note any logic in the ViewModel		No logic, only getters and setters2. Repos	a. List all repos being called by the factory		LessonRepo	b. List the repo methods being called directly		LessonRepo.GetById	c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this		There are none3. DbObjects	a. List the DbObjects being used	    	Lesson		Course		User	b. For each of these, list the properties being read		Lesson - ContainerLessonChildren, Lesson.Course		Course - Course.IsMaster, Course.User		User - User.DisplayName	c. Identify any of these properties that are from custom code in the DbObject		Course.IsMaster	d. For each of these DbObjects, indicate whether your new ViewObject should contain a DomainObject for this or just list a couple of properties		Lesson - we should return a DomainObject since it can link to other Objects		Course and User can be combined to return Name and boolean IsMaster4. GroupLessonDisplayViewModel is used. See GroupLessonDisplayViewModel	-------------------------------------------------------------------------------GroupNarrativeViewModel1. View Model: GroupNarrativeViewModel	a. Properties of the View Model 		i. The simple pass-through properties.			private int _groupSelectedLessonId;			private int _groupReferenceLessonId; 			private int _selectedLessonId;		ii. The rest: the needed output properties for the View Object. 			private string _heading;		    	private string _narrativeText;	b. Note any logic in the View Model 		No logic in the viewmodel.2. Repos 	a. List all repos being called by the factory 		LessonRepo	b. List the repo methods being called directly. 		LessonRepo.getByID		LessonRepo.GetIndividualNarrative	c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this. 		GetIndividualNarrative returns null if the lesson is a Master. It calls the narrative repo otherwise. 3. DbObjects 	a. List the DbObjects being used. 		Lesson		Narrative		Course		User	b. For each of these, list the properties being read. 		Lesson.Course		Course.User		Course.IsMaster		User.DisplayName	c. Identify any of these properties that are from custom code in the DbObject. 		Course.IsMaster	d. For each these DbObjects, indicate whether your new ViewObject should contain a DomainObject for this or just list a couple of properties.		Lesson - We should return the Domain Object. This links to the other Objects		Narrative - We should only return the narrative text.		Course  and User can be compressed to just return the username and the course boolean IsMaster 4. No other view Models are used in this Model-------------------------------------------------------------------------------GroupTabLessonViewModel1. View Model Name: GroupTabLessonViewModel	a. Properties of the View Model		i. The simple pass-through properties.			 int _lessonId		ii. The rest: the needed output properties for the View Object.			nt _courseId			bool _hasPersonalNarrative			bool _hasDifferentLessonPlan			bool _hasDifferentDocuments			string _title			bool _hasDifferentLessons	b. Note any logic in the View Model			No logic in this view model2. Repos	a. List all repos being called by the factory		LessonRepo	b. List the repo methods being called directly.		LessonRepo.GetById		LessonRepo.LessonHasIndividualNarative		LessonRepo.LessonsHaveDifferentLessonPlans		LessonRepo.LessonsHaveDifferentDocuments		LessonRepo.LessonsHaveDifferentChildLessons	c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this.		None of these are simple queries. They all use getById and LessonPlanRepo3. DbObjects	a. List the DbObjects being used.		Lesson		Course		User	b. For each of these, list the properties being read.		Lesson.Course		Course.User		Course.IsMaster		User.DisplayName	c. Identify any of these properties that are from custom code in the DbObject.		Course.IsMaster	d. For each these DbObjects, indicate whether your new ViewObject should   	    contain a DomainObject for this or just list a couple of properties.		Lesson – return the CourseDomObj since this links to other Objects		Course – just return the boolean value		User – just return the name of the user4. Other View Models	No other View Models are used in this Model-------------------------------------------------------------------------------GroupTabViewModel1. View Model Name: GroupTabViewModel	a. Properties of the View Model		i. The simple pass-through properties.			private int _groupSelectedLessonId			private int _groupReferenceLessonId			private int _ selectedLessonId			private bool_showGroupLessons			private bool _showGroupNarrative			private bool _showGroupLessonPlan			private bool _showGroupDocuments			private int _groupSelectedDocumentUseId		ii. The rest: the needed output properties for the View Object.			private List<GroupTabLessonViewModel> _lessonList			private string _groupReferenceLessonNameb. Note any logic in the View Model		No logic in GroupTabViewModel2. Repos	a. List all repos being called by the factory		LessonRepo	b. List the repo methods being called directly.		LessonRepo.getById		LessonRepo.lessonsThatShareMaster	c. If any of these methods are not simple queries, but can be traced back to simple queries, briefly describe this.		lessonsThatShareMaster calls getByID and applies some logic before running a simple query		3. DbObjects	a. List the DbObjects being used.		Lesson		Course	b. For each of these, list the properties being read.		Lesson.Name		Lesson.Course		Lesson.IsMaster		Lesson.Id		Course.UserId	c. Identify any of these properties that are from custom code in the DbObject.		Lesson.isMaster	d. For each these DbObjects, indicate whether your new ViewObject should   	    contain a DomainObject for this or just list a couple of properties.		Lesson – return the CourseDomObj since this links to other Objects		Course – just return the boolean value4. Other View Models	GroupTabLessonViewModel is used in this code. See GroupTabLessonViewModel